## User Prompt: Add this all subscription API in swagger

## AI Response: Added OpenAPI annotations to SubscriptionController (create, verify, status, cancel) and WebhookController (handle). These will appear under tags "Subscription" and "Razorpay Webhook" in Swagger UI.

