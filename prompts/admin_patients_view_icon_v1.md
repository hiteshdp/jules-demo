## User Prompt: in admin module , patient section , i need icon instead of view text

## AI Response: Updated `frontend/admin/src/pages/Patients.tsx` to replace the "View" text button with an Ant Design eye icon (`EyeOutlined`). Also added the corresponding icon import and kept actions consistent with other icon-only actions. Updated the generated-by header comment to reference this prompt file.

